paths:
  /v1/user:
    get:
      tags:
        - User
      summary: Get all users
      responses:
        '200':
          description: Get all users
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GetUsers'
            example:
              data:
                $ref: '#/components/schemas/GetUsers'
